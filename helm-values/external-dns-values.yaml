provider:
  name: cloudflare
env:
  - name: CF_API_TOKEN
    valueFrom:
      secretKeyRef:
        name: cloudflare-api-key
        key: cloudflare-api-key

sources:
  - service
  - ingress
  - gateway-httproute

policy: sync
registry: noop
# registry: txt
# --ignore-txt-owner-id